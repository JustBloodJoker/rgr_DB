import Controller as con
import Model as m

if __name__ == '__main__':
   contr = con.Controller()
   contr.run()
   # model = m.Model()

    #model.GenerateDataForEquipmentType(1000, 1)
   # model.GenerateDataForEquipment(1000, 1)
   # model.GenerateDataForClient(1000, 1)
   # model.GenerateDataForBooking(1000, 1)
   # model.GenerateDataForRental(1000, 1)

  #  res, tim = model.FindingTheMostRentedEquipmentType()
   # print(f"{res}   TIME : {tim}")
  #  res, tim = model.FindingTheCustomerWhoBookedTheEquipmentMostOften()
   # print(f"{res}   TIME : {tim}")
   # res, tim = model.FindingTheEquipmentThatIsMostOftenBooked()
   # print(f"{res}   TIME : {tim}")